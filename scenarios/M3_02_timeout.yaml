# M3_02 — Recovery Timeout

scenario_id: M3_02
name: recovery_timeout_no_candidate
duration_sec: 40.0
seed: 20261222
rates:
  sensor_hz: 10.0
  map_hz: 1.0
scan:
  generator: analytic
plant:
  enabled: false
amcl_surrogate:
  enabled: false
publish_initialpose: true

# Trigger active recovery, but no candidate arrives within max_attempt_duration (30s).
# Expected: MODEL-3 enters FAILED state, stops commanding motion.

phases:
  - id: NOMINAL
    start_sec: 0.0
    end_sec: 4.0
    gnss: {quality: 4, satellites: 12, hdop: 0.8, differential_age: 0.5, fix_available: true}
    lidar: {mode: normal, valid_fraction: 1.0, angle_min: -3.141592653589793, angle_max: 3.141592653589793, range_m: 3.0}
    amcl: {publish: true, covariance: 0.05, yaw_covariance: 0.03}
    odom: {publish: true, linear_x: 0.05}

  - id: TRIGGER_AND_HOLD_DEGRADATION
    start_sec: 4.0
    end_sec: 40.0
    gnss: {quality: 0, satellites: 0, hdop: 99.0, differential_age: 99.0, fix_available: false}
    lidar: {mode: narrow, valid_fraction: 0.3, angle_min: -0.05, angle_max: 0.05, range_m: 3.0}
    amcl: {publish: true, covariance: 0.90, yaw_covariance: 0.03}
    odom: {publish: true, linear_x: 0.0}
